# Gradle JVM settings - reduced for stability
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m

# Android settings
android.useAndroidX=true
android.enableJetifier=true

# PERMANENT FIX: Disable problematic Gradle features
org.gradle.daemon=false
org.gradle.parallel=false
org.gradle.configureondemand=false

# Kotlin compiler settings for stability
kotlin.incremental=false
kotlin.compiler.execution.strategy=in-process

# Java toolchain - will use system JAVA_HOME or PATH
# Set JAVA_HOME environment variable if needed
